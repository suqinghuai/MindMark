# 自动阅卷系统 (MindMark)

一个基于AI视觉大模型的自动化阅卷系统，能够自动截图、AI评分、填分并提交，实现批卷流程的全自动化。

## 📋 项目简介

本项目利用Python实现了一个自动化阅卷系统，通过以下步骤完成批量阅卷：
1. **自动截图**：对指定屏幕区域进行截图，获取学生答题内容
2. **AI评分**：调用魔搭社区的视觉大模型对答题内容进行智能评分
3. **自动填分**：将AI评分结果自动输入到评分系统
4. **自动提交**：点击提交按钮完成评分
5. **自动翻页**：点击下一题按钮，准备评阅下一份试卷

## ✨ 功能特点

- **全自动化**：从截图到提交全流程自动化，无需人工干预
- **AI智能评分**：利用先进的视觉大模型进行评分，评分标准可自定义
- **日志记录**：详细的日志记录，方便查看评分过程和结果
- **异常处理**：具备完善的异常处理机制，确保系统稳定运行
- **紧急停止**：鼠标移至屏幕左上角可紧急停止，保障系统安全
- **配置灵活**：通过配置文件可灵活调整各项参数

## 🛠️ 技术栈

- **Python 3.12+**
- **第三方库**：
  - `mss` - 高性能屏幕截图
  - `Pillow` - 图像处理
  - `pyautogui` - 自动化鼠标键盘操作
  - `openai` - 调用AI模型
  - `configparser` - 配置文件解析

## 📦 使用步骤

双击运行MindMark.exe即可启动系统。

## ⚙️ 配置说明

项目使用 `config.ini` 文件进行配置，主要包含以下部分：

### 1. 基础配置 (`[basic]`)
- `loop_count`：循环阅卷次数（0表示无限循环）
- `step_delay`：每步操作间隔（秒）
- `log_dir`：日志保存目录

### 2. 截图配置 (`[screenshot]`)
- `region_left`、`region_top`：截图区域左上角坐标
- `region_width`、`region_height`：截图区域宽度和高度
- `save_dir`：截图保存目录

### 3. AI配置 (`[ai]`)
- `api_key`：魔搭社区API密钥
- `base_url`：API接口地址
- `model`：使用的AI模型
- `grading_prompt`：评分标准（系统提示词）
- `max_retries`：API调用最大重试次数

### 4. 自动化配置 (`[automation]`)
- `score_input_x`、`score_input_y`：分数输入框坐标
- `submit_btn_x`、`submit_btn_y`：提交按钮坐标
- `next_btn_x`、`next_btn_y`：下一题按钮坐标

## 🚀 使用方法

1. **配置调整**：根据实际情况修改 `config.ini` 文件中的各项参数
   - 使用截图工具（如微信截图）获取准确的坐标值
   - 确保API密钥有效且有足够的调用额度

2. **启动系统**
   ```bash
   MindMark.exe
   ```

3. **系统运行**：
   - 启动后会有3秒倒计时，用于切换到阅卷浏览器窗口
   - 系统会自动开始截图、AI评分、填分、提交、翻页的循环
   - 运行过程中会在控制台和日志文件中输出详细信息

4. **停止系统**：
   - 按 `Ctrl+C` 手动终止
   - 或鼠标移至屏幕左上角触发紧急停止

## 📁 项目结构

```
auto2/
├── main.py          # 主入口文件
├── screenshot.py    # 截图功能模块
├── ai_grader.py     # AI评分模块
├── auto_click.py    # 自动化操作模块
├── config.ini       # 配置文件
├── requirements.txt # 依赖文件
├── logs/            # 日志目录
└── screenshot/      # 截图保存目录
```

## 📊 评分标准

系统默认使用以下评分标准（可在 `config.ini` 中修改）：

- **内容切题（4分）**：紧扣题目，有明确观点或情感，内容完整
- **结构逻辑（3分）**：段落清晰，语句连贯，逻辑通顺
- **语言表达（2分）**：用词准确，无明显错别字和标点错误
- **字数合规（1分）**：字数在180-220字之间

**评分参考**：
- 8-10分：切题、完整、通顺、优美，字数合规
- 5-7分：基本切题，结构清晰，语言通顺，存在少量问题
- 1-4分：跑题、内容残缺或语病较多，需大幅修改
- 0分：完全跑题或无有效内容

## 🔒 安全注意事项

1. **API密钥安全**：请妥善保管 `config.ini` 文件中的API密钥，避免泄露
2. **坐标准确性**：使用前请仔细校准各项坐标值，避免误操作
3. **系统稳定性**：运行过程中避免操作鼠标和键盘，以免干扰自动化流程
4. **紧急停止**：如发现异常，立即将鼠标移至屏幕左上角触发紧急停止

## 📝 日志说明

系统会在 `logs` 目录下生成日志文件，命名格式为 `grading_yyyyMMdd_HHmmss.log`，包含以下信息：
- 系统启动和停止时间
- 每道题的处理过程
- AI评分结果和原因
- 异常信息（如有）

## 🔧 故障排除

1. **截图失败**：检查截图区域坐标是否正确，确保目标区域在屏幕可见范围内
2. **AI评分失败**：检查API密钥是否有效，网络连接是否正常
3. **自动化操作失败**：检查坐标值是否准确，目标按钮是否可见
4. **系统无响应**：按 `Ctrl+C` 手动终止，或重启系统

## 🤝 贡献

欢迎提交 Issue 和 Pull Request 来改进这个项目！

## 📄 许可证

本项目仅供学习交流，禁止任何形式的商业使用，未经许可不得复制、分发。

---

**提示**：本系统仅用于辅助阅卷，最终评分结果请以人工审核为准。